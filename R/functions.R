# R/functions.R
# Define your R functions here. They will be automatically sourced by targets.
# Organize functions into multiple files as your project grows.

# Example function: clean raw data
# clean_raw_data <- function(data) {
#   data |>
#     janitor::clean_names() |>
#     drop_na()
# }

# Example function: fit a model
# fit_model <- function(data) {
#   lm(outcome ~ predictor1 + predictor2, data = data)
# }

# Example function: create a summary table
# create_summary_table <- function(data) {
#   data |>
#     group_by(category) |>
#     summarise(
#       n = n(),
#       mean_value = mean(value, na.rm = TRUE),
#       sd_value = sd(value, na.rm = TRUE)
#     )
# }
